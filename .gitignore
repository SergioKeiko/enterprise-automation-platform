# Secrets — never commit these
.env
*.env
!*.env.example
!.env.example

# Let's Encrypt certificate store (contains private keys)
n8n-stack/traefik/acme.json
*.pem
*.key
*.crt

# Runtime data — lives on the server only
n8n-stack/data/
n8n-stack/qdrant_data/
n8n-stack/uptime-kuma/
supabase/volumes/db/data/
supabase/volumes/storage/

# Backups & logs
backups/
*.log
*.bak
*.backup
*~

# OS noise
.DS_Store
Thumbs.db
